// Affichage du nouveau mail
document.getElementById("emails-list").getElementsByTagName('tr')[0].insertAdjacentHTML("beforebegin", "<%= j render 'email', email: @email %>");

// jQuery pour l'effet d'animation
$(document).ready(function() {
	$('.to-fade').first().hide();
	$('.to-fade').first().fadeIn('slow');
});
